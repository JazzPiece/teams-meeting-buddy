# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# AI assistant local state (permissions, session data)
.claude/
.cursor/
.aider*

# Working/scratch files (not part of the extension)
teams-transcript-extension/working.js
*.orig
*.rej
*.bak
*.tmp

# Browser extension packages and signing material
*.crx
*.pem
*.xpi
*.zip
*.pfx
*.p12
web-ext-artifacts/
build/
dist/

# Node (if tooling is added later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnp.*
.yarn/

# Env / secrets
.env
.env.*
*.key
*.secret
*.p8
id_rsa*
secrets.*

# Logs
*.log

# Exported transcripts and other test output. These contain real meeting
# content and real names. Never commit them. Un-ignore a specific file with
# !name if one is ever genuinely needed.
coverage/
*.vtt
*.srt
*.docx
*.txt
*Meeting_Recording*
*Meeting Recording*

# Stray screenshots and captures at the repo root. Store assets live in
# store-assets/ and are added deliberately.
/image*.png
/Screenshot*.png
/*.jpg
/*.jpeg
/*.gif
/*.mp4
